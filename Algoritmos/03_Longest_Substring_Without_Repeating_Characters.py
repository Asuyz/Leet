class Solution(object):
    def lengthOfLongestSubstring(self, s):
        lista = []
        count = 0

        for valor_indice in s:
            if valor_indice in lista

            lista.append(valor_indice)



                


            
sol = Solution()
print(sol.lengthOfLongestSubstring

        # Criar uma logica que compara se a letra de tras é igual a atual, se for parar a logica, se não for continuar até que aconteça isso.
        # 3 condicionais for if e while.